# Conventions

## Markdown (Python-Markdown for mkdocs)

- Always leave a blank line between a paragraph and a list
- Use 4-space indentation for all nested content under list items
- Use `-` for unordered lists (not `*`)
- Indent fenced code blocks by 4 spaces per nesting level inside lists
- Run `make lint` before committing docs changes
